apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus            # stable UID referenced by dashboards/adr-evidence.json
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
